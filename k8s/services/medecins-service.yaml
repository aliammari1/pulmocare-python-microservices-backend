# Medecins (Doctors) Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: medecins-service
  namespace: medapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: medecins-service
  template:
    metadata:
      labels:
        app: medecins-service
    spec:
      containers:
        - name: medecins-service
          image: your-registry/medapp-medecins:latest
          ports:
            - containerPort: 8081
          env:
            - name: PORT
              value: "8081"
            - name: OTEL_SERVICE_NAME
              value: "medecins-service"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=medecins-service"
          envFrom:
            - configMapRef:
                name: medapp-config
            - secretRef:
                name: medapp-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: medecins-service
  namespace: medapp
spec:
  selector:
    app: medecins-service
  ports:
    - port: 8081
      targetPort: 8081
